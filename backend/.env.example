# ============================================
# ETCHI PLATFORM - BACKEND
# Arquivo de exemplo de variáveis de ambiente
# ============================================
# Copie este arquivo para .env e preencha com seus valores
# cp .env.example .env

# ============================================
# AMBIENTE GERAL
# ============================================
NODE_ENV=development
PORT=3000
API_URL=http://localhost:3000/api
CLIENT_URL=http://localhost:3000

# ============================================
# BANCO DE DADOS
# ============================================
# MongoDB URI (local ou Atlas)
MONGODB_URI=mongodb://localhost:27017/etchi_db
# MongoDB URI Local (fallback)
MONGODB_URI_LOCAL=mongodb://localhost:27017/etchi_db

# ============================================
# AUTENTICAÇÃO JWT
# ============================================
# Secret para tokens JWT (use uma string longa e aleatória)
JWT_SECRET=seu-secret-jwt-muito-seguro-aqui-mude-em-producao
JWT_REFRESH_SECRET=seu-refresh-secret-muito-seguro-aqui-mude-em-producao
JWT_EXPIRES_IN=24h
JWT_REFRESH_EXPIRES_IN=7d

# ============================================
# GOOGLE MAPS API
# ============================================
# Chave da API do Google Maps para geocodificação e rotas
GOOGLE_MAPS_API_KEY=sua-chave-google-maps-api-aqui

# ============================================
# FIREBASE (Push Notifications)
# ============================================
# Opção 1: Usar arquivo de service account (RECOMENDADO)
# Caminho para o arquivo JSON de service account do Firebase
GOOGLE_APPLICATION_CREDENTIALS=/path/to/firebase-service-account.json

# Opção 2: Usar variáveis de ambiente (alternativa)
# Se não usar GOOGLE_APPLICATION_CREDENTIALS, configure estas:
FIREBASE_PROJECT_ID=seu-firebase-project-id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@seu-projeto.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# ============================================
# PAGAMENTOS
# ============================================
# Multicaixa Express
MULTICAIXA_API_URL=https://api.multicaixa.ao
MULTICAIXA_API_KEY=sua-chave-api-multicaixa

# PayPal
PAYPAL_API_URL=https://api.sandbox.paypal.com
PAYPAL_CLIENT_ID=seu-paypal-client-id
PAYPAL_CLIENT_SECRET=seu-paypal-client-secret
PAYPAL_ACCESS_TOKEN=seu-paypal-access-token

# ============================================
# EMAIL (SMTP)
# ============================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=seu-email@gmail.com
SMTP_PASS=sua-senha-de-app
SMTP_FROM=Etchi Platform <noreply@etchi.ao>

# ============================================
# CORS
# ============================================
# URLs permitidas para CORS (separadas por vírgula)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:5173

# ============================================
# UPLOAD
# ============================================
# Diretório para uploads
UPLOAD_DIR=./uploads
# Tamanho máximo de arquivo (em bytes)
MAX_FILE_SIZE=5242880
# Tipos de arquivo permitidos (separados por vírgula)
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/jpg,application/pdf

# ============================================
# RATE LIMITING
# ============================================
# Janela de tempo para rate limiting (em ms)
RATE_LIMIT_WINDOW_MS=900000
# Número máximo de requisições por janela
RATE_LIMIT_MAX=100

# ============================================
# LOGGING
# ============================================
# Nível de log (error, warn, info, debug)
LOG_LEVEL=info

# ============================================
# SEGURANÇA
# ============================================
# Bcrypt rounds para hash de senhas
BCRYPT_ROUNDS=12

# ============================================
# NOTAS IMPORTANTES
# ============================================
# 1. NUNCA commite o arquivo .env no repositório
# 2. Use valores diferentes em desenvolvimento e produção
# 3. Para produção, use variáveis de ambiente do servidor
# 4. Mantenha os secrets seguros e rotacione-os periodicamente
# 5. Para Firebase, prefira usar GOOGLE_APPLICATION_CREDENTIALS
